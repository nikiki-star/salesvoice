# 核心依赖
openai>=1.40          # LLM 调用（任何 OpenAI 兼容端点，默认 DeepSeek）
PyYAML>=6.0
requests>=2.31
sherpa-onnx>=1.10     # 本机离线 ASR（SenseVoice），纯 ONNX，无需 torch

# 可选：证据溯源抽取的第二引擎（google/langextract，Apache-2.0）
# langextract>=0.1

# 可选：说话人分离自动标注（需 HuggingFace token 并接受 pyannote 模型条款）
# pyannote.audio>=3.1
# torch>=2.0
